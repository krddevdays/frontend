import localFont from 'next/font/local';

export const brutalType = localFont({ src: './brutal-type-bold.woff2', weight: '700' });
export const hagrid = localFont({ src: './hagrid-bold.woff2', weight: '800' });
